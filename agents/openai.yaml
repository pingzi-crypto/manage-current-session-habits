interface:
  display_name: "Current Session Habits"
  short_description: "Scan this Codex thread for habit candidates and confirm or ignore them in place"
  default_prompt: "Use $manage-current-session-habits when the user wants to scan the current conversation for repeated shorthand or explicit phrase definitions, add or ignore 第N条 candidates, list or delete existing user habit phrases, or stop low-value habit cleanup without leaving the current thread."
