interface:
  display_name: "Session Habit Manager"
  short_description: "Scan and confirm habit phrases in the current Codex thread"
  default_prompt: "Use $manage-current-session-habits when the user wants to scan this conversation for habit candidates, add 第1条, ignore 第1条, or list/delete existing user habit phrases without leaving the current Codex thread."
